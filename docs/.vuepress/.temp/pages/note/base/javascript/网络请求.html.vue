<template><div><h1 id="网络请求" tabindex="-1"><a class="header-anchor" href="#网络请求" aria-hidden="true">#</a> 网络请求</h1>
<h2 id="" tabindex="-1"><a class="header-anchor" href="#" aria-hidden="true">#</a> </h2>
<table>
<thead>
<tr>
<th>属性</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>readyState</td>
<td>当前响应和请求处于哪个阶段。</td>
</tr>
<tr>
<td>response</td>
<td>响应正文。</td>
</tr>
<tr>
<td>responseType</td>
<td>返回响应数据的类型。是一个可枚举的属性。它可能有以下几种类型: 'arraybuffer'、'blob'、'document'、'json'、'text'，当时空字符串的时候默认设置为'text'.</td>
</tr>
<tr>
<td>responseText</td>
<td>获取的是服务端返回的字符串形式的响应体。</td>
</tr>
<tr>
<td>responseURL</td>
<td>返回响应的序列化URL。</td>
</tr>
<tr>
<td>responseXML</td>
<td>只有当响应内容类型&quot;&quot;text\xml&quot;或者是&quot;application\xml&quot;时才会有内容。</td>
</tr>
<tr>
<td>status</td>
<td>响应的HTTP状态。</td>
</tr>
<tr>
<td>statusText</td>
<td>响应的HTTP状态描述。</td>
</tr>
<tr>
<td>timeout</td>
<td>超时时间。代表在多少毫秒内如果没有返回请求就自动终止。默认值为0，意味着没有超时。</td>
</tr>
<tr>
<td>upload</td>
<td>该属性返回的是一个XMLHttpRequestUpload对象，用来获取上传的进度。</td>
</tr>
<tr>
<td>withCredentials</td>
<td>该值是一个Boolean类型的值，表示是否该使用类似cookies、authorization header。</td>
</tr>
</tbody>
</table>
<table>
<thead>
<tr>
<th style="text-align:left">方法</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">open(method, url, async)</td>
<td>初始化一个请求。</td>
</tr>
<tr>
<td style="text-align:left">send(body)</td>
<td>发送请求，如果该请求是异步的，则该方法将在请求发送后立刻返回。</td>
</tr>
<tr>
<td style="text-align:left">abort()</td>
<td>如果请求被发出，则立刻终止请求。</td>
</tr>
<tr>
<td style="text-align:left">setRequestHeader(header, value)</td>
<td>设置请求头，在open()方法和send()方法之间调用。</td>
</tr>
<tr>
<td style="text-align:left">overrideMimeType(mimeType)</td>
<td>重写服务器返回的MIME类型。</td>
</tr>
<tr>
<td style="text-align:left">getAllResponseHeaders()</td>
<td>以字符串形式返回所用用CRLF分隔的响应头。</td>
</tr>
<tr>
<td style="text-align:left">getResponseHeader(name)</td>
<td>返回包含指定值的响应头</td>
</tr>
</tbody>
</table>
<table>
<thead>
<tr>
<th>状态</th>
<th>含义</th>
<th>改变时机</th>
</tr>
</thead>
<tbody>
<tr>
<td>0</td>
<td>未初始化（Uninitialized）</td>
<td>尚未调用open()方法。</td>
</tr>
<tr>
<td>1</td>
<td>已打开(Open)</td>
<td>调用open()方法之后。</td>
</tr>
<tr>
<td>2</td>
<td>已发送(Send)</td>
<td>调用send()方法之后。</td>
</tr>
<tr>
<td>3</td>
<td>接收中(receiving)</td>
<td>已经收到部分响应。</td>
</tr>
<tr>
<td>4</td>
<td>已完成(Complete)</td>
<td>已经接收到全部响应。</td>
</tr>
</tbody>
</table>
<table>
<thead>
<tr>
<th>事件</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>ontimeout()</td>
<td>在请求超时的情况下触发。</td>
</tr>
<tr>
<td>onreadystatechange()</td>
<td>当readystate发生改变之后触发。</td>
</tr>
</tbody>
</table>
<table>
<thead>
<tr>
<th>事件</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>onloadstart()</td>
<td>当接收第一个字节时触发。</td>
</tr>
<tr>
<td>onload()</td>
<td>在成功接收到响应之后触发。</td>
</tr>
<tr>
<td>onloadend()</td>
<td>当通信完成之后触发。且在onload、onabort、onerror之后触发。</td>
</tr>
<tr>
<td>onprogress()</td>
<td>在接收响应期间反复触发。</td>
</tr>
<tr>
<td>onabort()</td>
<td>在调用abort()终止连接时触发。</td>
</tr>
<tr>
<td>onerror()</td>
<td>在请求出错时触发。</td>
</tr>
</tbody>
</table>
<h2 id="fetch" tabindex="-1"><a class="header-anchor" href="#fetch" aria-hidden="true">#</a> Fetch</h2>
<h4 id="header" tabindex="-1"><a class="header-anchor" href="#header" aria-hidden="true">#</a> Header</h4>
<h4 id="request" tabindex="-1"><a class="header-anchor" href="#request" aria-hidden="true">#</a> Request</h4>
<h4 id="response" tabindex="-1"><a class="header-anchor" href="#response" aria-hidden="true">#</a> Response</h4>
<h4 id="混入的body属性" tabindex="-1"><a class="header-anchor" href="#混入的body属性" aria-hidden="true">#</a> 混入的body属性</h4>
<h2 id="websocket" tabindex="-1"><a class="header-anchor" href="#websocket" aria-hidden="true">#</a> WebSocket</h2>
</div></template>


